<resources>

    <!-- tema translúcido para evitar a tela de splash ao iniciar -->
    <style name="Translucent" parent="android:Theme.Translucent.NoTitleBar" />
    
    <style name="DeviceDefault" parent="android:Theme.DeviceDefault" >
        
        <!-- remove a linha (sombra) logo abaixo da action bar -->
        <item name="android:windowContentOverlay">@null</item>
        
    </style>
    
    <style name="DeviceDefault.Light" parent="android:Theme.DeviceDefault.Light" >
        
        <!-- remove a linha (sombra) logo abaixo da action bar -->
    	<item name="android:windowContentOverlay">@null</item>
    	
    </style>
    
    <style name="DeviceDefault.Light.Xperia" parent="android:Theme.DeviceDefault.Light" >
        
        <!-- remove a linha (sombra) logo abaixo da action bar -->
    	<item name="android:windowContentOverlay">@null</item>
    	
    	<!-- xperia gray bars (adicionar com o apk manager)
    	<item name="com.sonyericsson.uxp:extendedLookAndFeel">true</item>
    	-->
        
    </style>
	
</resources>
